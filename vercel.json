{
    "rewrites": [
      {
        "source": "/api/(.*)",
        "destination": "/api"
      }
    ],
    "builds": [
      {
        "src": "frontend/index.html",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "frontend/dist" 
        }
      }
    ]
  }